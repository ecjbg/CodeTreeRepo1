/*
bool a = true;
bool b = true;

cout << (a && b); // Output: 1
cout << "\n";

a = true;
b = false;

cout << (a && b); // Output: 0
//
bool a = true;
bool b = false;

cout << (a || b); // Output: 1
cout << "\n";

a = false;
b = false;

cout << (a || b); // Output: 0
//
bool a = false;

cout << !a;   // Output: 1
cout << "\n";

a = true;

cout << !a;   // Output: 0
//
example
bool is_raining = true;
bool have_umbrella = false;

// && operator: Returns true only if both conditions are true
cout << (is_raining && have_umbrella);  // Output: 0
cout << "\n";

// || operator: Returns true if at least one condition is true
cout << (is_raining || have_umbrella);  // Output: 1
cout << "\n";

// ! operator: Returns the opposite value of the condition
cout << !is_raining;                // Output: 0
*/
#include <iostream>
using namespace std;

int main() {
    // Please write your code here.
    int a=1;
    int b=2;
    char c='C';
    cout<<a<<"->"<<b<<"->"<<c;
    return 0;
}